# Handoff — Smart Investment Monitor 完整 I/O 驗證

## 狀態：進行中（約 60% 完成）

## 本次 Session 已完成的工作

### 1. 修正的 BUG（已 commit 到 backend.py）

| Bug | 根因 | 修正 |
|-----|------|------|
| migrate-positions 手續費爆量（千萬級） | positions.shares 存「股」但 `_calc_trade_costs` 乘 1000（當「張」） | Line ~1540: 加 heuristic `shares >= 1000` 時先 `÷1000` |
| SNDK 被錯標 market='TW' | migrate 用 `code.isalpha()` 判斷而非讀 position.market 欄位 | Line ~1530: 改為 `SELECT ... market FROM positions`，優先用 position 的 market |
| 11 筆 trade records commission 值錯誤 | 上述 bug 產生的 | 已直接修正 DB 數據 |

### 2. 已驗證通過的功能（手動 curl 確認 input→output 正確）

- **交易費用計算**：BUY commission=price×張×1000×0.001425×(1-discount) ✓ / SELL +交易稅 ✓
- **持倉同步**：BUY 加碼加權平均 ✓ / SELL 扣減 ✓ / 全賣→closed ✓
- **持倉 CRUD**：POST 新增 ✓ / PUT 編輯 verify ✓ / DELETE verify ✓
- **交易紀錄 CRUD**：POST BUY/SELL ✓ / PUT 編輯 ✓ / DELETE ✓ / analytics ✓ / migrate ✓
- **IC 推薦市場隔離**：TW refresh 不蓋 US ✓ / US refresh 不蓋 TW ✓
- **IC batch-score**：KD/MACD/RS/OBV/MFI 指標齊全 ✓ / score 0-100 ✓ / direction valid ✓
- **Expert session stuck 修復**：無 running 卡住 ✓ / 重啟自動標 error ✓
- **Macro lock 切換**：lock/unlock → risk-level 正確反映 ✓
- **EXIT_D 掃描**：正確偵測跌破 5% ✓（3363 -10.62%, 3017 -6.32%）
- **自選股 TW CRUD**：add/remove verify ✓
- **自選股 US CRUD**：add/remove ✓
- **K-bar 資料**：2330 回傳 334 根日K + MA ✓ / US TSLA 124 bars ✓
- **Snapshot/Sparkline**：回傳正確 ✓
- **策略管理**：list 21 策略 ✓ / toggle on/off verify ✓ / params update ✓
- **回測**：run 回傳 summary+trades+equity_curve ✓（0 trades — 需確認是否為資料不足）
- **Expert**：sessions/roles/config/schedules 全部 ✓
- **IC sources**：system sources ✓ / text add 需 `name` 欄位 ✓
- **風控設定**：risk-config GET ✓ / macro-lock toggle ✓
- **資料管理**：data/stats ✓
- **系統透視**：datasources 50 ✓ / feature-map 20 ✓ / formula-registry 38 ✓
- **盤後**：after-hours scan ✓ / chip fetch ✓ / daytrade-warn ✓ / signal history 26 ✓
- **通知**：notify/test ✓（telegram+email 皆回 true）
- **板塊輪動**：11 sectors ✓
- **總經解讀**：macro/interpretation 有 environment/label/advice ✓
- **Token usage**：today_tokens/today_cost ✓
- **前後端路徑對齊**：94 前端 API 路徑全數對應 127 後端路由 ✓
- **頁面 DOM**：12 個 page div 全部存在 ✓

### 3. 尚未驗證的功能（剩餘 ~40%）

已寫好完整測試腳本 `tests/test_full_io.py`（覆蓋 141 個 endpoint），但尚未跑完。

**未測 endpoint 清單（按頁面分）：**

#### K 線頁
- `GET /api/vwap/{code}` — 回傳結構未驗
- `GET /api/tick-stats/{code}` — 回傳結構未驗
- `GET /api/indicators/batch` — 回傳結構未驗
- `GET /api/market-data/status` / `GET /api/market-data/{code}` — 未驗

#### 持倉頁
- `GET /api/positions/{pid}/exit-check` — 未驗
- `PUT /api/positions/{pid}/lifecycle` — 未驗
- `POST /api/us/positions` — 未驗

#### 盤後分析
- `GET /api/chip/squeeze-candidates` / `squeeze-breakout` / `itrust-lock` / `abandon` — 未驗
- `GET /api/chip/{code}` — 單股籌碼明細未驗
- `GET /api/chip/daytrade/{code}` — 未驗
- `POST /api/chip/fetch-daytrade` — 未驗
- `GET /api/chip/scheduler-status` / `POST toggle` — 未驗
- `GET /api/news/{code}` / `news/bearish-reversal` / `POST news/fetch-material` — 未驗

#### 資訊中心量化工具
- `POST /api/ic/multi-factor` — 未驗
- `POST /api/ic/factor-generate` / `factor-ic` — 未驗
- `GET /api/ic/factors/{code}` — 未驗
- `GET /api/ic/options/{code}` — 未驗
- `GET /api/ic/crypto/{symbol}` — 未驗
- `GET /api/ic/events/{code}` — 未驗
- `GET /api/ic/social-sentiment/{code}` — 未驗
- `POST /api/ic/auto-quant` — 未驗
- `POST /api/ic/analyze` — AI 深度解讀未驗
- `POST /api/ic/backtest-preview` — 未驗

#### 資訊中心資料來源
- `POST /api/ic/sources` — 新增自訂來源未驗
- `POST /api/ic/sources/upload` — 檔案上傳未驗
- `DELETE /api/ic/sources/{db_id}` — 刪除來源未驗
- `POST /api/ic/sources/{db_id}/fetch` — 單源抓取未驗
- `POST /api/ic/sources/fetch-all` — 全部抓取未驗
- `GET /api/ic/sources/news` — 新聞來源未驗
- `GET /api/ic/kb/search` / `kb/entities` — 知識庫未驗
- `GET /api/ic/openbb/status` — 未驗

#### 資訊中心設定
- `POST /api/ic/settings` — 寫入未驗
- `POST /api/ic/notify-config` — 寫入未驗

#### 系統設定
- `GET /api/auto-sell/status` — 未驗
- `POST /api/auto-sell/toggle/{state}` / `toggle-exitc/{state}` — 未驗
- `POST /api/auto-sell/execute` / `exit-c` — 未驗
- `POST /api/risk-config` — 寫入未驗

#### 回測
- `GET /api/backtest/{bt_id}` — 單筆詳情未驗
- `POST /api/backtest/walk-forward` — 未驗

#### GitHub
- `GET /api/github/status` / `watch` — 未驗
- `POST /api/data/cleanup` / `GET data/integrity` — 未驗

#### Formula Registry
- `POST /api/formula-registry/params` — 參數修改未驗
- `POST /api/formula-registry/reset` — 重置未驗

### 4. 測試腳本

`tests/test_full_io.py` — 完整 141 endpoint 測試腳本，跑法：
```bash
TOKEN=$(curl -s http://localhost:8765/api/auth/token | python -c "import sys,json; print(json.load(sys.stdin).get('token',''))")
python tests/test_full_io.py "$TOKEN"
```

腳本有一個已知 fix：`is_ok()` 已修正支援 list 回傳。直接跑即可。

### 5. 其他本 session 完成的工作（前一段 context）

- **Expert session stuck fix**：`_expert_run_session` 全 body 包 try/except/finally
- **IC 推薦 UI 改版**：TW/US tab 分離，不再互蓋
- **showPage bug fix**：4 處 `showPage('page-xxx')` → `showPage('xxx')`
- **HTML 使用手冊**：`ui/manual.html`
- **Markdown 使用手冊**：`docs/2026-06-21-user-manual.md`
- **E2E 測試新增**：T8-06（stuck session check）、T8-07（completed_at check）

### 6. 環境

- Backend: `python backend.py`（localhost:8765）
- ngrok: `ngrok http 8765 --domain=hanky-doorway-constable.ngrok-free.dev`
- 兩者皆已重啟正常運行
- DB: `ui/monitor.db`（SQLite）
- Token: 每次重啟重新產生，從 `/api/auth/token` 取得

### 7. 用戶要求

**「整個專案做完驗證，所有功能（包含 tab）input/output 都要驗證過，做完再來回報」**

下一個 session 請：
1. 直接跑 `test_full_io.py`
2. 檢查 FAIL 項目
3. 修正任何 bug
4. 一次性回報完整結果，不要中途停下
